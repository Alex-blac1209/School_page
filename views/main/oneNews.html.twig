{% extends 'base.html.twig' %}

{% block body %}
    <div class="profile">
        <div class="info">
            <h3>{{ news.title }}</h3>
            <p>{{ news.description }}</p><br />
            <p>Autor: {{ author.name }}</p><br />
            {% if news.path != "" %}
                <br />
                <br />
                <a href="{{ news.path }}">Przejdź pod url</a>
            {% endif %}
        </div>
    </div>
{% endblock body %}

{% block css %}
    <link rel="stylesheet" type="text/css" href="/styles/profile.css" />
{% endblock css %}